{
  "name": "vcx-cli",
  "version": "0.3.2",
  "description": "Named Vercel CLI accounts on one device",
  "type": "module",
  "bin": {
    "vcx": "dist/cli.js"
  },
  "files": [
    "dist",
    "install.sh",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && bun -e \"require('node:fs').chmodSync('dist/cli.js', 0o755)\"",
    "clean": "bun -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "test": "bun run clean && bun run build && node --test test/cli.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": [
    "vercel",
    "cli",
    "accounts",
    "profiles"
  ],
  "license": "MIT",
  "author": "akrupa-appto",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ApptoAI/vcx.git"
  },
  "bugs": {
    "url": "https://github.com/ApptoAI/vcx/issues"
  },
  "homepage": "https://github.com/ApptoAI/vcx#readme",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cross-spawn": "^7.0.6"
  },
  "devDependencies": {
    "@types/cross-spawn": "^6.0.6",
    "@types/node": "^20.17.57",
    "typescript": "^5.9.3"
  }
}
